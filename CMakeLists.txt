CMAKE_MINIMUM_REQUIRED(VERSION 3.0)


IF(CMAKE_BUILD_TYPE MATCHES "Release" OR CMAKE_BUILD_TYPE MATCHES "RelWithDebInfo")
  SET(INSTALL_LOCATION_IS_SYSTEM 1)
ENDIF()

ENABLE_TESTING()

ADD_SUBDIRECTORY(src)
ADD_SUBDIRECTORY(tests)
